---
description: Generate the Oracle — the card-facts file Checks run against — from the Export in this Collection home
---

Use the `oracle` skill from the tutor plugin (read `${CLAUDE_PLUGIN_ROOT}/skills/oracle/SKILL.md` and follow it) to build `oracle.jsonl` from the Export.
