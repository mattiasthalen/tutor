---
description: Start a Build from a settled Brief — generate the Suite and show it honestly red before any card is picked
argument-hint: [brief name]
---

Use the `build` skill from the tutor plugin (read `${CLAUDE_PLUGIN_ROOT}/skills/build/SKILL.md` and follow it) to start the Build, beginning from what the user already said: $ARGUMENTS
