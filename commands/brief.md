---
description: Settle a Brief — the statement of intent for a new Deck — before any card is picked
argument-hint: [deck idea]
---

Use the `brief` skill from the tutor plugin (read `${CLAUDE_PLUGIN_ROOT}/skills/brief/SKILL.md` and follow it) to settle a Brief with the user, starting from what they already said: $ARGUMENTS
