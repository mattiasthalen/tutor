---
description: Review a finished Deck along the two axes — Standards and Brief — ending with a Review Block and its computed Verdict
argument-hint: [deck name]
---

Use the `review` skill from the tutor plugin (read `${CLAUDE_PLUGIN_ROOT}/skills/review/SKILL.md` and follow it) to review the Deck, starting from what the user already said: $ARGUMENTS
