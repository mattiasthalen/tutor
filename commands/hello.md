---
description: Confirm the tutor plugin is installed and responding
---

Use the `hello` skill from the tutor plugin (read `${CLAUDE_PLUGIN_ROOT}/skills/hello/SKILL.md` and follow it) to confirm the installation.
